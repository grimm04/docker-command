# /world is dir

docker volume create hello

docker run -d -v hello:/world busybox ls /world

docker volume inspect hello


docker volume ls

# Run a Temporary Container:

docker run --rm -it -v myvolume:/app/files busybox 
cd /app/files
ls -l